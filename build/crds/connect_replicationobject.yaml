---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    nexus: |
      {"name":"ReplicationObject.connect","hierarchy":["nexuses.api.nexus.org","configs.config.nexus.org","connects.connect.nexus.org","replicationconfigs.connect.nexus.org"],"nexus-rest-api-gen":{"uris":null}}
  creationTimestamp: null
  name: replicationobjects.connect.nexus.org
spec:
  conversion:
    strategy: None
  group: connect.nexus.org
  names:
    kind: ReplicationObject
    listKind: ReplicationObjectList
    plural: replicationobjects
    shortNames:
    - replicationobject
    singular: replicationobject
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              group:
                type: string
              hierarchical:
                type: boolean
              hierarchy:
                properties:
                  Path:
                    type: string
                required:
                - Path
                type: object
              kind:
                type: string
              localRuntime:
                type: boolean
              name:
                type: string
            required:
            - group
            - kind
            - name
            - localRuntime
            - hierarchical
            - hierarchy
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions:
  - v1
