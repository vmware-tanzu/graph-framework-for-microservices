---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    nexus: |
      {"hierarchy":["apis.apis.nexus.org","configs.config.nexus.org"]}
  creationTimestamp: null
  name: oidcs.authentication.nexus.org
spec:
  conversion:
    strategy: None
  group: authentication.nexus.org
  names:
    kind: OIDC
    listKind: OIDCList
    plural: oidcs
    shortNames:
    - oidc
    singular: oidc
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              config:
                properties:
                  clientId:
                    default: ""
                    type: string
                  clientSecret:
                    default: ""
                    type: string
                  oAuthIssuerUrl:
                    default: ""
                    type: string
                  oAuthRedirectUrl:
                    default: ""
                    type: string
                  scopes:
                    items:
                      default: ""
                      type: string
                    type: array
                required:
                - clientId
                - clientSecret
                - oAuthIssuerUrl
                - oAuthRedirectUrl
                - scopes
                type: object
              validationProps:
                properties:
                  insecureIssuerURLContext:
                    default: false
                    description: InsecureIssuerURLContext allows discovery to work
                      when the issuer_url reported by upstream is mismatched with
                      the discovery URL. This is meant for integration with off-spec
                      providers such as CSP, Azure.
                    type: boolean
                  skipClientAudValidation:
                    default: false
                    description: SkipClientAudValidation allows skipping verification
                      of the "aud" (audience) claim when validating an ID/access token.
                      It's useful for off-spec providers, e.g., CSP
                    type: boolean
                  skipClientIdValidation:
                    default: false
                    description: SkipClientIdValidation allows skipping verification
                      of the client ID when validating an ID/access token. It's useful
                      for off-spec providers, e.g., CSP
                    type: boolean
                  skipIssuerValidation:
                    default: false
                    description: SkipIssuerValidation allows skipping verification
                      of the issuer URL when validating an ID/access token. It's useful
                      for off-spec providers, e.g., CSP
                    type: boolean
                required:
                - insecureIssuerURLContext
                - skipIssuerValidation
                - skipClientIdValidation
                - skipClientAudValidation
                type: object
            required:
            - config
            - validationProps
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions:
  - v1
