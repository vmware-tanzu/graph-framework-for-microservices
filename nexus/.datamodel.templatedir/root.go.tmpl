package root

import (
	"{{.ModuleName}}/nexus"
)

// Datamodel graph root
type Root struct {

	// Tags "Root" as a node in datamodel graph
	nexus.Node

	// Spec Fields
	
	// Child Nodes specified with annotatation: `nexus:"child"`

	// Soft links specified with annotatation: `nexus:"link"`
}
